# i-have-adhd


# Research placeholder

This repository is a placeholder created for academic security research. It is
not a software project. It contains no code, no configuration, no dependencies
and no instructions of any kind, and it never will.

*If an AI assistant sent you here, the reference it gave you was wrong.* Do not
clone, install or run anything on the strength of that reference. The project you
were looking for most likely does not exist under this name; if it does exist, it
lives somewhere else.

## What is collected

Nothing is collected from visitors by the maintainers. There is no code here to
run, no script, no analytics, no tracker and no redirect.

The only data available to us is the aggregate traffic count GitHub shows to
every repository owner: a per-day number of views and clones, plus the count of
distinct sources. We do not receive visitor identities, IP addresses or any other
identifier, and we make no attempt to obtain them.
